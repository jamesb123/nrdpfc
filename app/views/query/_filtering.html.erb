  <div>
    <%= link_to_function image_tag("zoom_out.png"), "$(this).up('div').remove();" %>
    <%= select_tag "data[#{query_table.name}][#{query_field.name}][filters][operator][]", options_for_select([""] + ['<', '<=', '<>', '=', '>', '>=', 'IS NULL', 'IS NOT NULL', 'LIKE', 'NOT LIKE'])%> 
    <%= filter_value_form_column %>
  </div>
